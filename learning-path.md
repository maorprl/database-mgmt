# מסלול למידה — בסיס נתונים

**מקור עיקרי:** קורס "בסיס נתונים 31-028-24", רואי זרחיה, שנקר — בית הספר לתעשייה וניהול
13 הרצאות, `github.com/maorprl/database-mgmt`

**נקודת המוצא:** אתה יודע לזהות שמשהו שגוי בנתונים, ואין לך את הבסיס לגזור למה.
**המטרה:** לעבוד בשיטה במסד לא מוכר, לא באינטואיציה.

---

## החלוקה

**חובה = משהו אחר נשען על זה, או שזה מסביר משהו שכבר נתקלת בו.**

| | זמן |
|---|---|
| **חובה** | **~5.5 שעות** — יום אחד |
| העשרה | ~8 שעות — לפי צורך |

הכל השאר — סקרנות, לא בסיס.

---

# חובה

## 1. הרצאה 1, עמודים 3–7 — 15 דק'

**דלג על עמ' 1–2** (מנהלות, ציונים, נוכחות).

| מושג | למה חובה |
|---|---|
| התפתחות המודלים — היררכי, רשתי, רלציוני | למה המודל הטבלאי ניצח. זה ההקשר שכל השאר יושב בו |
| SQL היא שפה **הצהרתית** | מתארת **מה** ולא **איך**. ולכן יש שכבת ביצוע נפרדת — משם ההבדל בין `EXISTS` ל-`JOIN` |
| תהליך הרצת שאילתה (עמ' 7) | מה ה-DBMS עושה מרגע הכתיבה עד התשובה |

**בלי זה, "מודל רלציוני" מופיע פתאום בהרצאה 2 בלי הקשר.**

---

## 2. הרצאות 2–3 — σ ו-π — כשעה וחצי

**σ (בחירה)** — מסנן שורות. `WHERE`. מספר השורות יורד או נשאר.

**π (הטלה)** — בוחר עמודות. `SELECT`.

**וכאן הדבר הלא-צפוי:** כשזורקים עמודות, שורות שהיו שונות הופכות זהות — ואם התוצאה היא קבוצה, הן קורסות לאחת.

> **למה זה חובה:** מכאן נובע למה `EXISTS` לא מסוגל לשכפל שורות, ולמה `SELECT DISTINCT` **לא** היה פותר לך את הבאג. שתי השאלות שנשארו פתוחות בשיחה.

---

## 3. הרצאה 4 — הפרש (−) — 30 דק'

`R1 − R2` — כל מה שב-R1 ואינו ב-R2.

זה ה-`NOT EXISTS`. **ה-anti-join שהחזיר לך 27.34 מיליארד.**

> **ומה שנובע מזה:** אם `A` מתפצל ל-"יש התאמה" ו-"אין התאמה", שתי הקבוצות **זרות ואיחודן שלם**. לכן הסכומים שלהן חייבים להתחבר לסך.
>
> וכשזה לא קרה — 68.69 + 27.34 יצא 96.03 בעוד הסך היה 96.09 — זו לא הייתה תחושה. **זו הפרה של זהות מתמטית**, ולכן ידעת בוודאות שהפרדיקטים אינם זהים. זה מה שחשף את `'USA'` ו-`'US'`.

---

## 4. הרצאה 7 — מכפלה קרטזית ו-JOIN — שעה

**זו ההרצאה המרכזית. אם תעשה רק אחת — את זו.**

**מכפלה קרטזית (×)** — כל שורה מול כל שורה, בלי תנאי. `|A × B| = |A| × |B|`.

**ו-JOIN אינו פעולה בסיסית:**

```
A ⋈ B  =  σ(A × B)
```

הכפלה, ואז סינון על התנאי ב-`ON`.

### מה שההרצאה לא מכסה — ה-fan-out

בסכמה שלך, **שני** יחסי 1:M לאותה טבלת אב:

```
companies → addresses    (1:M)
companies → funding      (1:M)
```

וכשמחברים את שניהם:

```
|companies ⋈ addresses ⋈ funding| = Σ a(c) × f(c)
```

כל סבב מופיע `a(c)` פעמים. **וההפרש בסכומים:**

```
פנטום = Σ (a(c) − 1) · (סך הגיוס של c)
```

**חברה עם כתובת אחת נותנת `a − 1 = 0`.** רק חברות עם שתי כתובות ומעלה מייצרות את הבעיה.

### תגזור על הנתונים שלך

| כתובות | חברות | סך הגיוס ($B) | (a−1)·סכום |
|---|---|---|---|
| 1 | 898 | 56.39 | 0 |
| 2 | 68 | 8.93 | 8.93 |
| 3 | 16 | 2.57 | 5.14 |
| 4 | 3 | 0.16 | 0.48 |
| 5 | 1 | 0.00 | 0.01 |
| 6 | 1 | 0.64 | 3.21 |
| | | | **17.77** |

**המדידה נתנה 17.74.** ההפרש הוא עיגול.

898 חברות — **91% מהאוכלוסייה, 82% מהכסף** — תורמות **אפס**. חברה בודדת עם 6 משרדים תורמת 3.21 מיליארד.

**גודל החברה לא רלוונטי. רק מספר הכתובות.**

---

## 5. הרצאה 13 — נרמול — שעה

**דלג על החלק הראשון** (טריגרים). רק נרמול.

נרמול הוא פירוק טבלאות כדי למנוע כפילויות ואנומליות עדכון.

> **למה זה חובה:** זו התשובה לשאלה "**למה `company_addresses` היא טבלה נפרדת מלכתחילה**". ובלי להבין את זה, קימבל בשלב הבא חסר משמעות.

**הערה:** ההרצאה נשענת על מפתחות (הרצאה 10). אתה עובד איתם יומיומית, אז כנראה לא תצטרך אותה — אבל אם משהו לא ברור, שם התשובה.

---

## 6. קימבל, פרק 1 — Grain — שעתיים

*The Data Warehouse Toolkit*, פרק 1 בלבד.

| מושג | מה זה נותן |
|---|---|
| **Grain** | ההגדרה של מה מייצגת שורה. ההחלטה הראשונה בתכנון, לפני הכל |
| **Fact ו-Dimension** | `funding` היא fact (אירועים), `companies` ו-`addresses` הן dimensions (הקשר) |
| **Fan trap** | **השם הרשמי לבאג שלך** — חיבור שתי dimensions לאותה fact |

**וההיפוך שהופך אותו למעניין:** נרמול מלמד לפרק טבלאות. קימבל מלמד **מתי לא** — כי במחסן נתונים, שנועד לניתוח ולא לעדכון, כפילות מבוקרת עדיפה על JOINs מרובים.

**אי אפשר להבין את זה לפני נרמול.** לכן הוא אחרון.

> **אזהרה:** קימבל כותב "עשה כך", לא "הנה למה". אחרי זרחיה זה ירגיש אחורה. **קרא אותו כאוצר מילים** — ההסבר כבר יהיה לך.

---

## 7. פרוטוקול האבחון — שעה

לא תיאוריה. **צ'קליסט למסד לא מוכר.**

**1. מיפוי קרדינליות לפני שאילתה אחת**
```sql
SELECT COUNT(*) AS rows, COUNT(DISTINCT <fk>) AS entities FROM <table>;
```
`rows > entities` = יחס 1:M. **שתי טבלאות 1:M לאותו אב = fan-out מובטח.**

**2. אימות עצמאי של השלם**
תמיד למדוד את הסך בנפרד, לא לחבר את החלקים.
`A + B ≠ סך` = יש קבוצה שנופלת בין הכיסאות.

**3. פרופיל של כל שדה שמסננים לפיו**
```sql
SELECT <col>, COUNT(*) FROM <table> GROUP BY <col> ORDER BY 2 DESC LIMIT 50;
```
לפני שכותבים `WHERE col = 'X'`. חושף וריאציות, רווחים, NULLים.

**4. `COUNT(*)` מול `COUNT(col)`**
ההפרש הוא מספר ה-NULLים. הרגל קבוע.

**5. בדיקת שפיות על כל פלט**
ספירות עקביות? יותר חברות מסבבים זה בלתי אפשרי.

**6. שתי דרכים לאותו מספר**
זה מה שגילה את ה-$17.74B. אין תחליף.

**7. השאלה על מה שלא נמצא**
"האם השדה ממולא כי הוא נכון, או כי מישהו טרח?"
```sql
SELECT COUNT(*) AS total, COUNTIF(<col> IS NULL) AS nulls,
       COUNT(DISTINCT <col>) AS distinct_vals
FROM <table>;
```

**הרץ את שבעת השלבים על טבלה שאתה לא מכיר.** זה המבחן היחיד שבאמת נחשב.

---

# העשרה

לא נשען עליהן כלום. קח לפי צורך או סקרנות.

| מקור | נושא | למה לא חובה |
|---|---|---|
| זרחיה 3–5 | איחוד, חיתוך, חילוק | לא נשען עליהם כלום. חיתוך נגזר מהפרש: `A ∩ B = A − (A − B)`. חילוק כמעט לא בשימוש |
| זרחיה 5–6 | `GROUP BY`, `HAVING` | אתה כותב את זה יומיומית |
| זרחיה 8 | Outer join, `NULL` | שימושי מאוד, אבל לא שורש. `NULL` דורש הרחבה של המודל |
| זרחיה 9 | תתי-שאילתות | שימושי, לא בסיס |
| זרחיה 10 | מפתחות ראשי וזר | אתה עובד איתם בפועל |
| זרחיה 11–12 | אינדקסים, אילוצים, `Views`, `Alter` | עלים בעץ |
| קימבל 2 | סכמת כוכב, SCD | לבניית DWH, לא לניתוח |
| CMU 15-445, הרצאות 10–12 | ביצוע שאילתות, אלגוריתמי JOIN | למה `EXISTS` גם **מהיר** יותר. באנגלית |
| Lohr, פרקים 3–6 | דגימה — ריבוד, PPS, FPC | **ענף נפרד לגמרי.** לא נשען על שום דבר כאן ולא תומך בכלום. ראה `docs/case-study.md` §7 |

---

# סדר ומעקב

```
1 (עמ' 3-7)  →  2-3  →  4  →  7  →  13  →  קימבל 1  →  פרוטוקול
   15 דק'      1.5 שע'  30ד'  1שע'  1שע'    2 שע'      1 שע'
```

| # | מה | ☐ |
|---|---|---|
| 1 | הרצאה 1, עמ' 3–7 | ☐ |
| 2 | הרצאות 2–3 — σ, π | ☐ |
| 3 | הרצאה 4 — הפרש | ☐ |
| 4 | **הרצאה 7 — JOIN ו-fan-out** | ☐ |
| 5 | הרצאה 13 — נרמול | ☐ |
| 6 | קימבל פרק 1 — grain | ☐ |
| 7 | פרוטוקול האבחון | ☐ |

---

# הערות

**למה הסדר הזה.** לא לפי קושי ולא לפי שימושיות — **לפי תלות**. σ ו-π מסבירים למה `EXISTS` לא משכפל; הפרש מסביר את ה-anti-join; מכפלה מסבירה את ה-fan-out; נרמול מסביר למה הסכמה מפוצלת; קימבל מסביר מתי לא לפצל. כל אחד נשען רק על מה שלפניו.

**אם יש לך שעה אחת בלבד — הרצאה 7.** היא הכי קרובה למה שכבר קרה לך, וגם בלי הבסיס המלא תרוויח ממנה.

**ולא לכל דבר יש סיבה.** `'USA '` עם רווח נגרר קיים כי מישהו הדביק מאקסל. אין שם עומק. הכישור הוא לזהות מהר לאיזה סוג הבעיה שייכת — ולא לבזבז שעה על מקרה שהתשובה עליו היא "כי ככה".
