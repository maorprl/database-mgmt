# בסיס נתונים — חומרי קורס

קורס **בסיס נתונים 31-028-24**, רואי זרחיה
שנקר — בית הספר לתעשייה וניהול, הפקולטה להנדסה

13 הרצאות, 129 עמודים.

---

## מה יש כאן

| # | קובץ | עמ' | נושא |
|---|---|---|---|
| 1 | `lecture01.pdf` | 7 | מבוא, DBMS, מודלים, SQL כשפה הצהרתית |
| 2 | `lecture02.pdf` | 9 | אלגברת יחסים — פתיחה |
| 3 | `lecture03.pdf` | 9 | בחירה (σ), הטלה (π), איחוד (∪) |
| 4 | `lecture04.pdf` | 8 | הפרש (−), חיתוך, ופעולות נוספות |
| 5 | `lecture05.pdf` | 10 | חילוק (÷), סיום אלגברה. פונקציות הקבצה, `GROUP BY` |
| 6 | `lecture06.pdf` | 11 | `HAVING`, `DISTINCT` עם `COUNT`, איחוד ב-SQL |
| 7 | `lecture07.pdf` | 14 | **מכפלה קרטזית, `JOIN`, `ON` ו-`USING`, `Outer Join`** |
| 8 | `lecture08.pdf` | 15 | `Outer Join` עם `NULL`, טיפול ב-`NULL`, `INSERT`/`UPDATE`/`DELETE` |
| 9 | `lecture09.pdf` | 13 | תתי-שאילתות |
| 10 | `lecture10.pdf` | 7 | מפתח ראשי, מפתח זר |
| 11 | `lecture11.pdf` | 8 | אינדקסים, אילוצים, `Alter` |
| 12 | `lecture12.pdf` | 10 | `Drop`, תתי-שאילתות ב-DDL, `Views` |
| 13 | `lecture13.pdf` | 8 | טריגרים, **נרמול טבלאות** |

---

## מסלול מקוצר — לניתוח נתונים

הקורס המלא מיועד למי שבונה מסד נתונים. **מי שמנתח מסד קיים צריך פחות.**

החלוקה למטה לפי **תלות**, לא לפי שימושיות:
**חובה = משהו אחר נשען על זה.**

### חובה — ~5.5 שעות

| # | מה | זמן | למה |
|---|---|---|---|
| 1 | הרצאה 1, **עמ' 3–7 בלבד** | 15 דק' | התפתחות המודלים; SQL היא שפה **הצהרתית** — מתארת מה ולא איך |
| 2 | הרצאות 2–3 | 1.5 שע' | **σ ו-π.** מכאן נובע למה `EXISTS` לא מסוגל לשכפל שורות |
| 3 | הרצאה 4 | 30 דק' | **הפרש (−)** = `NOT EXISTS`. וממנו: שתי הקבוצות זרות ואיחודן שלם |
| 4 | **הרצאה 7** | 1 שע' | **מכפלה קרטזית.** `A ⋈ B = σ(A × B)` — JOIN אינו פעולה בסיסית |
| 5 | הרצאה 13, **נרמול בלבד** | 1 שע' | למה טבלאות מפוצלות מלכתחילה |
| 6 | קימבל, פרק 1 | 2 שע' | **Grain**, fact/dimension, ומתי **לא** לנרמל |

**דלג בהרצאה 1 על עמ' 1–2** (מנהלות, ציונים).
**דלג בהרצאה 13 על טריגרים.**

### העשרה — לפי צורך

| מקור | למה לא חובה |
|---|---|
| 3–5: איחוד, חיתוך, חילוק | לא נשען עליהם כלום. חיתוך נגזר מהפרש: `A ∩ B = A − (A − B)`. חילוק כמעט לא בשימוש מעשי |
| 5–6: `GROUP BY`, `HAVING` | בשימוש יומיומי — לומדים בעשייה |
| 8: `Outer Join`, `NULL` | שימושי מאוד, אבל לא שורש |
| 9: תתי-שאילתות | שימושי, לא בסיס |
| 10: מפתחות | מובן בפועל למי שעובד עם הסכמה |
| 11–12: אינדקסים, `Views`, טריגרים | עלים בעץ — לא תומכים בכלום |

---

## אם יש שעה אחת

**הרצאה 7.**

היא מכסה את מכפלה קרטזית ו-JOIN, ובה נמצא ההסבר לבאג הנפוץ ביותר בעבודה עם סכמה רלציונית:

```
companies → addresses    (1:M)
companies → funding      (1:M)
```

חיבור שני יחסי 1:M לאותה טבלת אב מייצר מכפלה חלקית:

```
|companies ⋈ addresses ⋈ funding| = Σ a(c) × f(c)
```

וכשמסכמים סכומים, כל רשומה נספרת `a(c)` פעמים:

```
ניפוח = Σ (a(c) − 1) · (הסכום של c)
```

**הבאג שקט** — אין שגיאה, אין אזהרה, והתוצאה נראית סבירה לחלוטין.

> **הרצאה 7 מלמדת את המכניקה של JOIN, אך לא את ה-fan-out כתופעה.** זהו פער שכדאי להיות מודע אליו.

---

## מקורות משלימים

| מקור | מה מוסיף |
|---|---|
| Kimball, *The Data Warehouse Toolkit*, פרקים 1–2 | Grain, fact/dimension, fan trap. **קרא כאוצר מילים** — הוא כותב "עשה כך" ולא "הנה למה" |
| CMU 15-445 (Andy Pavlo, יוטיוב, חינם) | הרצאות 10–12: אלגוריתמי JOIN, למה `EXISTS` גם **מהיר** יותר |
| Silberschatz, *Database System Concepts*, פרק 2 | אלגברה רלציונית בפורמליזם מלא |

---

## קרדיט

כל הזכויות על חומרי הקורס שמורות לרואי זרחיה.
[LinkedIn](https://www.linkedin.com/in/zerahiaroei/) · [שנקר](https://www.shenkar.ac.il/he/people/roei-zerahia)

הריפו הזה הוא עותק אישי לצורכי לימוד.
